general:
  branches:
    only:
      - master

machine:
  java:
    version: oraclejdk8

dependencies:
  override: 
    - mvn dependency:resolve-plugins --update-snapshots 
    - mvn dependency:resolve --update-snapshots

test:
  override:
    - mvn test